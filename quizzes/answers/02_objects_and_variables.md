# Answers 2

1. **B.** Both names reference the Array mutated by `<<`.
2. **C.** `equal?` tests whether the receiver and argument are the same object.
3. **B, C.** `upcase!` and `concat` mutate; a mutator need not end in `!`.
4. **B.** Blocks can reassign an existing outer local.
5. **C.** Constants hold references; reassignment and object mutation are separate operations.

