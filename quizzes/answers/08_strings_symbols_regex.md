# Answers 8

1. **A.** `sub` replaces only the first match.
2. **A, B.** `%i` and explicit colon literals create Symbols.
3. **B.** Ruby 3.1 returns a one-character String.
4. **A, D.** Absolute anchors guarantee the complete String. `^` and `$` are line anchors and may match within a multiline String.
5. **B.** No valid numeric prefix begins the String, so `to_i` returns zero.

