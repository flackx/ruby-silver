# Answers 10

1. **B.** Class `new` allocates and then invokes instance method `initialize`.
2. **C.** Ordinary classes default to Object.
3. **A, C.** Inclusion participates in instance-method lookup and ancestry.
4. **B.** Bare `super` forwards current `y`, whose subclass default supplied 2.
5. **C.** `attr_accessor` defines both reader and writer.

