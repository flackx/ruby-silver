# Answers 7

1. **A, B.** `filter` aliases `select`.
2. **B.** `find` returns the first matching element.
3. **C.** `tally` returns a Hash keyed by the original String elements.
4. **A, C.** Both produce descending natural numeric order.
5. **B.** `Comparable` builds relational methods on the class's `<=>` implementation.

