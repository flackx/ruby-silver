# Answers 1

1. **B, C.** Only `nil` and `false` are falsey.
2. **B, C.** A local begins with lowercase or underscore, may contain digits later, and cannot be a keyword.
3. **B.** A three-dot Range excludes its endpoint.
4. **A.** Both ranges contain 2, but `case` stops at the first matching `when`.
5. **C.** `puts` prints `x` and returns `nil`; the surrounding `p` prints that return value.

