# Answers 6

1. **A.** Start at index 1 and take length 2.
2. **A, C.** `push` and `concat` change their receivers.
3. **A, C.** `key?` and `member?` check keys; `value?` checks values and `contain?` does not exist.
4. **B.** The second argument is `fetch`'s fallback.
5. **C.** The block runs separately for each position.

