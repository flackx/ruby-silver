# Answers 13

1. **B.** `||=` assigns when the old value is falsey, including `false`.
2. **A, B.** Non-bang map and reject return new Arrays.
3. **A.** Hash `include?` checks keys.
4. **B.** The String is mutated without rebinding the constant.
5. **A, C.** Undefined method calls and zero division fail at runtime; invalid literal and missing `end` fail parsing.

