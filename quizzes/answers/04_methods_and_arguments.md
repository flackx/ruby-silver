# Answers 4

1. **C.** The last evaluated expression is returned implicitly.
2. **B.** `**` expands a Hash into keyword arguments.
3. **C.** Definition-side `*x` gathers positional arguments into an Array.
4. **B.** `x` receives 5 and omitted `y` uses 2.
5. **A, C.** Missing required and excess positional arguments are argument errors.

