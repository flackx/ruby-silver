# Answers 9

1. **A.** Quotient 2 and remainder 1.
2. **B.** `Integer()` is strict; `to_i` reads the valid prefix 8.
3. **A, C.** String inherits from Object; exact instance class is String. Object includes Kernel.
4. **A, B.** Both append modes force writes to the end.
5. **A, C.** `pwd` is on Dir and `basename` is on File.

