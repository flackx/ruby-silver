# Answers 3

1. **A.** Low-precedence `and` makes this `(x = true) and false`.
2. **A, B.** `&&` and `||` may skip the right operand; `&` and `|` do not.
3. **B.** The remainder of 8 divided by 3 is 2.
4. **B.** Array `&` returns the intersection.
5. **A, C.** Numeric `==` treats these values as equal; negating falsey `nil` yields `true`.

