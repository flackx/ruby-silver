# Answers 1: Language basics

1. `0`. `nil` is falsey, so `||` evaluates and returns its right operand. Zero is truthy.
2. `:banana`. `1..4` includes 4 and the first matching `when` wins.
3. `3`. `Integer#times` returns its integer receiver, not the last block value.
4. Syntax error: `9` is not a valid digit in an octal integer literal.
5. `banana` followed by a newline. An empty Array is truthy.

