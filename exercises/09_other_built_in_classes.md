# Exercises 9: Other built-in classes

Runnable companion: [`runnable/09_other_built_in_classes.rb`](runnable/09_other_built_in_classes.rb)

1. What happens? `p Integer('42x')`

2. `p(('a'...'d').to_a)`

3. `p ['x'.is_a?(Object), 'x'.instance_of?(Object)]`

4. Which file modes truncate an existing file before writing: `r+`, `w`, `w+`, `a`?

5. What happens? `p Dir.basename('a/b')`
